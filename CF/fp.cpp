#include <bits/stdc++.h>

using namespace std;

int const N = 101;

int n, k;
int arr[N];

void solve(){
	cin >> n >> k;
	
}

int main(){
	ios_base::sync_with_stdio(0); cin.tie(nullptr); cout.tie(nullptr);	
	int t; cin >> t;
	while(t--) solve();
	return 0;
}
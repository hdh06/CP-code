#include <bits/stdc++.h>

using namespace std;

typedef pair<int, int> P;
#define st first
#define nd second
typedef long double ldb;

ldb solve(ii a, ii b, ii c){
	
}

int main(){
	int n; cin >> n;
	vector<P> arr(n);
	for (auto &x: arr) cin >> x;
	
	for (int i = 0; i < n; i++)
		for (int j = i + 1; j < n; j++)
			for (int k = i + 1; k < n; k++){
				
			}
	return 0;
}
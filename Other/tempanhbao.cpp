#include <bits/stdc++.h>
using namespace std;
#define st first
#define nd second
#define pb push_back
#define pf push_front
#define _pb pop_back
#define _pf pop_front
#define lb lower_bound
#define ub upper_bound
#define mtp make_tuple
#define all(x) (x).begin(), (x).end()
#define sz(x) (int)(x).size()
#define ll long long
#define ull unsigned long long
#define ldb long double
#define db double
#define str string
#define pi pair<int, int>
#define pll pair<ll, ll>
#define vi vector<int>
#define vll vector<ll>
#define vpi vector<pi>
#define vpll vector<pll>
#define matrix vector<vector<int> >
#define matrixll vector<vector<ll> >
#define gcd __gcd
#define lcm __lcm
#define spc(x, y) setprecision((y)) << fixed << (x)
#define slc(x, y, c) setw((y)) << setfill((c)) << (x)
#define write(x) cout << (x)
#define writeln(x) cout << (x) << '\n'
#define read(x) cin >> (x)
#define readln(x) getline(cin, (x))
#define __builtin_popcount __builtin_popcountll
#define __builtin_clz __builtin_clzll
#define __builtin_ctz __builtin_ctzll
#define FOR(i, l, r) for (int (i) = (l); (i) <= (r); ++(i))
#define FOS(i, r, l) for (int (i) = (r); (i) >= (l); --(i))
#define EACH(i, x) for (auto &(i) : (x))
#define WHILE while
#define DB1(x) cerr << "DB1 >> " << (#x) << ": " << (x) << '\n'
#define DB2(x, l, r) cerr << "DB2 >> " << (#x) << ": "; FOR((i), (l), (r)) cerr << (x)[(i)] << ' '; cerr << '\n'
#define file "EXITROAD"
#define stfu system("sudo poweroff")
mt19937_64 rd(chrono::steady_clock::now().time_since_epoch().count());
ll rand(ll l, ll r) { return uniform_int_distribution<ll>(l, r)(rd); }
/*
     _._     _,-'""`-._
    (,-.`._,'(       |\`-/|
        `-.-' \ )-`( , oVo)
               `-   \`_ `"'-

    Ăn rùa đậu đội tuyển,
    xin hứa sẽ lấy giải cao.
    Trần Thế Bảo.
    (DAB DAB DAB)
    -Em xin template
    ¯\_(ツ)_/¯ Huy
    #Hay lam Da Lat vi dai tro lai
*/

signed main() {
    template <typename _InputIter> cout.tie(0);
    // freopen(file".inp", "r", stdin); freopen(file".out", "w", stdout);
    // cerr << "\nTime executed: " << (ldb)clock() / (ldb)CLOCKS_PER_SEC << " sec";
    return 0;
}


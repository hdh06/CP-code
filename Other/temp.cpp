#include <bits/stdc++.h>
using namespace std;
#define st first
#define nd second
#define pb push_back
#define pf push_front
#define _pb pop_back
#define _pf pop_front
#define lb lower_bound
#define ub upper_bound
#define mtp make_tuple
#define all(x) (x).begin(), (x).end()
#define sz(x) (int)(x).size()
#define ll long long
#define ull unsigned long long
#define ldb long double
#define db double
#define str string
#define pi pair<int, int>
#define pll pair<ll, ll>
#define vi vector<int>
#define vll vector<ll>
#define vpi vector<pi>
#define vpll vector<pll>
#define gcd __gcd
#define lcm __lcm
#define FOR(i, l, r) for (int (i) = (l); (i) <= (r); ++(i))
#define FOS(i, r, l) for (int (i) = (r); (i) >= (l); --(i))
#define EACH(i, x) for (auto &(i) : (x))
#define WHILE while
#define DB1(x) cerr << "DB1 >> " << (#x) << ": " << (x) << '\n'
#define DB2(x, l, r) cerr << "DB2 >> " << (#x) << ": "; for (int (i) = (l); (i) <= (r); ++(i)) cerr << (x)[(i)] << ' '; cerr << '\n'
#define file "TEST"
mt19937_64 rd(chrono::steady_clock::now().time_since_epoch().count());
ll rand(ll l, ll r) { return uniform_int_distribution<ll>(l, r)(rd); }
/*
    DM năm nay tôi rớt đội tuyển, 
    năm sau xin thề ít nhất phải lấy giải 3.
    Trần Thế Bảo.
    (TEMPLATE CÀNG DÀI => SỰ HẬN THÙ CÀNG CAO)
*/
signed main() {
    ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    // freopen(file".inp", "r", stdin), freopen(file".out", "w", stdout);
    cerr << "Time executed: " << (ldb)clock() / (ldb)CLOCKS_PER_SEC << " sec";
    return 0;
}
